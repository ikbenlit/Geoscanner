// Firebase exports for GEO Scanner v2.0

// Client-side Firebase
export { auth } from '../firebase';

// Firebase Auth functions
export * from './auth';

// Firebase Admin (server-side only)
export * from './admin'; 